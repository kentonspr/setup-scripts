#!/usr/bin/env bash

export SETUP_GROUPS=("headless" "pihole")
export HOSTNAME=pihole

# Uncomment any of the below lines to skip its step
export INC_SUDO=true
export INC_SSH=true
export INC_NVIM=true
